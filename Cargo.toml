[package]
name = "toy"
version = "0.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rust_decimal = "1.36"
thiserror = "2.0"
tokio = { version = "1.43", features = ["sync", "rt", "macros", "rt-multi-thread"] }
csv = "1"
serde = { version = "1", features = ["derive"] }
